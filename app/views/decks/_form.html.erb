<%= form_with model: deck do |form| %>
  <div>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
    <% deck.errors.full_messages_for(:name).each do |message| %>
      <div><%= message %></div>
    <% end %>
  </div>

  <div>
    <%= form.submit %>
  </div>
  
<% end %>

