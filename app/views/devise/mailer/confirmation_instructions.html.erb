<p class="mb-3">Welcome <%= @email %>!</p>

<p class="mb-3">You can confirm your account email through the link below:</p>

<p class="mb-3"><%= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token), class: "btn btn-primary" %></p>
